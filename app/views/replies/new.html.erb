<h1>New Reply to Comment by <%= link_to @reply.comment.user.name, @reply.comment.user %> in Post "<%= link_to @reply.comment.post.title, @reply.comment.post %>"</h1>

<%= render 'form', reply: @reply %>

<div class=footer>
  <p>
    <%= link_to 'Back to Comment', @reply.comment %>
  </p>
</div>

<h1><%= link_to 'Comment', @reply.comment %></h1>
<%= render 'comments/comment', comment: @reply.comment %>

<h1><%= link_to 'Original Post', @reply.comment.post %></h1>
<%= render 'posts/post', post: @reply.comment.post %>
